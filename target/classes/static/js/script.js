console.log('Bookstore App: JS Connected');

// Example function to show interaction
function addToCart(bookId) {
    console.log('Adding book to cart:', bookId);
    // Future implementation: fetch('/cart/add/' + bookId) ...
    alert('Book added to cart (Demo)!');
}
